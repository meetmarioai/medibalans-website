node_modules
.staging
.env
inject_nav_v3.py
inject_sitemap_kroppsskanning.py
inject_d3_nav.py
deemoji_site.py
add_iv_reverse_links.py
.vercel
__pycache__/
*.pyc
.mcp.json
docs/halsodeklaration.md

# Written by scripts/swap-booking-block.mjs when the iframe is swapped out.
# Holds the exact removed markup so --revert restores it verbatim rather than
# reconstructing it. Local recovery state, not source.
scripts/.iframe-backup.json
